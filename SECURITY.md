# 安全与隐私

RSS Reader v1.0.0 是本地桌面插件，不提供开发者中转服务器，不收集遥测。

## 网络请求

- RSS 更新访问用户配置的订阅 URL。
- 用户开启标题翻译后，可见标题会直接发送给 Google 非正式网页翻译接口。
- 用户主动执行 LLM 推荐复核时，待判断条目的标题、摘要及用户填写的兴趣描述会发送给其配置的 OpenAI 兼容服务。
- “打开原文”交给系统默认浏览器。

## 本地数据

订阅、文献、状态、译文和推荐结果保存在 Vault 插件目录的 SQLite 数据库中。设置保存在本地 `data.json`。

LLM API Key 未额外加密，因此：

- 不写入日志或迁移报告
- 不从旧软件自动导入
- 不应提交到 Git 或包含在发布包中
- 使用共享 Vault 或同步服务时，应自行评估同步范围

## 翻译风险

免密网页翻译属于实验性辅助阅读功能，可能限流、失效或受地区政策影响。译文不保证学术准确性，不应用于正式引用。翻译失败不会覆盖原标题，也不会阻塞其他功能。

## 数据恢复

- 备份与恢复目录必须位于当前 Vault 内。
- 恢复前插件自动保存当前数据库恢复点。
- 数据库无法打开时，插件进入恢复模式，不覆盖原数据库。
- v1.0.0 的历史 GUID 修复只合并由预发布版本错误产生的非旧版 GUID 记录，并保留旧 GUID、用户状态和关联。
