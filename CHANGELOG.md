# Changelog

## 1.6.0 — 2026-08-24

- 设置新增“文献卡片”分组，可分别控制期刊、作者、发表日期、DOI、文本摘要和摘要图；
  标题、推荐依据和操作区始终保留。旧设置自动补齐默认值，升级后继续显示期刊和摘要图，
  保持 v1.5.0 的默认外观。
- 卡片高度改为根据全局启用行槽统一计算：标题固定三行，作者一行，文本摘要三行；
  单篇字段缺失或摘要图加载状态不会导致列表高度跳动。
- 发表日期按界面语言格式化并在无有效日期时回退到年份；作者、DOI 和摘要使用安全文本
  渲染，超长内容保留完整悬停文本。
- 文本摘要保持 RSS 原文且不触发翻译；仅含出版日期、来源、作者或 DOI 标签的订阅摘要
  不作为正文显示，也不修改数据库原文或推荐特征。
- 全字段卡片改为紧凑等高布局：期刊、日期和 DOI 左对齐连续排列，作者与摘要使用统一标签列，
  正文起点保持一致；标题、推荐依据、操作区和摘要图的位置与行为不变。
- 修复点击“感兴趣”、归档、隐藏或恢复后阅读器跳回顶部的问题，状态操作现在保留当前滚动位置。
- 关闭摘要图后不创建卡片图片元素或远程请求；启用时继续使用懒加载、失败隐藏和键盘可访问
  的放大预览。本版本不修改数据库 schema。

## 1.5.0 — 2026-08-17

- 新增 RSS/Atom 原生摘要图支持，按 media content、media thumbnail、图片
  enclosure、Atom enclosure 和摘要 HTML 图片的顺序解析；只接受 HTTP(S) 图片，
  不抓取文章页面。
- 数据库升级到 schema 5，增加 `items.image_url`。v3 数据库依次执行 v4、v5
  migration，v4 数据库只执行 v5；迁移前继续创建保护备份，旧文献图片保持空值。
- 文献卡片改为固定高度的内容/图片双分区，标题预留三行，相关度、关键词和操作按钮使用
  稳定底行；摘要图支持懒加载、失败隐藏和按图片尺寸放大预览。
- 标题支持混合纯文本与 LaTeX 公式，使用 Obsidian 原生 MathJax 渲染；标题和期刊文本
  恢复选择与复制。
- 修复旧 OPML 导入将 `xmlUrl` / `htmlUrl` 属性片段保存为订阅名称的问题；导入只读取
  RSS URL，异常历史名称可由文章级期刊或后续完整更新恢复。
- 订阅管理只显示一个期刊字段；订阅管理和兴趣分析表格统一增加滚动容器、固定表头、
  列对齐和状态样式。

## 1.4.1 — 2026-08-13

- 修复 Obsidian 社区扫描器将 Grid 的 `column-gap` 识别为部分支持的
  `multicolumn` 特性而产生的兼容性警告；改用等价的
  `gap: 0 var(--size-4-2)`，不改变文献卡片布局。
- 支持以 `<rdf:RDF>` 为根节点的 RSS 1.0 订阅，修复 Nature 期刊 RSS 被误报为无效
  RSS/Atom 的问题。
- 将 `node:sqlite` 显式配置为 esbuild external，修复 Node.js 22.23.2
  干净环境无法解析该模块而导致的社区构建验证失败；CI 新增同版本独立构建任务，避免
  Node.js 24 的 builtin 列表掩盖兼容问题。

## 1.4.0 — 2026-08-13

- 将桌面数据库底座从 `sql.js` 迁移到原生 `node:sqlite` `DatabaseSync`，启用 WAL、外键、忙等待、串行写队列和 SQLite Backup API；不满足 Node.js 22.16+ 运行时能力时阻止载入。
- v3 数据库自动原地升级到 schema 4，升级前在当前数据目录的 `backups/` 创建保护备份，迁移失败自动回滚；恢复处理 incoming/rollback 和 WAL/SHM sidecar。
- 分离订阅默认期刊名与 RSS 文章级期刊名，支持多订阅去重展示；更新订阅名称不会批量改写文章级数据，并加强 DOI、链接和标题/作者/年份去重兼容。
- 保留 v3 文献的既有 GUID；无 DOI 的普通论文继续以标题、年份和作者作为主身份，ScienceDirect 无作者条目使用 PII 区分；首次更新通过 PII 和规范化 URL 复用旧记录，并忽略 `dgcid` 跟踪参数，避免升级后重新插入已处理文献。
- 统一推荐训练、验证和正式评分的 TF-IDF/L2 特征尺度，升级特征版本，修复类别计数、模型时间排序和旧模型清理。
- 加强 Feed、推荐、翻译和 LLM 后台任务的 stop/drain/generation 取消契约；取消或卸载后不再写入数据库或刷新过期 UI。翻译缓存按目标语言区分，失败占位可重试。
- RSS/XML 响应限制为 10 MiB、拒绝 `DOCTYPE` 和无效根结构；链接限制为 HTTP(S)，只移除已知跟踪参数；LLM 限制为 HTTPS 或本机 HTTP，并支持超时、重试和严格结果解析。
- 文献卡片不再显示关键词解释中的上下文条目；标题固定预留两行，相关度改为左侧彩色状态框、右侧正负关键词两行布局，减少卡片高度和内容位置跳动。
- 启动自动更新会跳过一小时内成功更新过的订阅并在通知中显示数量；手动更新始终执行，失败退避不计入“近期更新”提示。设置保存增加 debounce；同步更新中英文 README、开发说明、安全说明、故障排查和 CI/Release Node.js 24 流程。
- 新增维护者文档导航、架构设计和数据库设计，集中说明模块依赖、生命周期、表关系、数据所有权、迁移及恢复规则；发布说明保持简洁的中英文用户摘要。

## 1.3.0 — 2026-08-02

- 设置页迁移到 Obsidian 1.13 声明式 `getSettingDefinitions()` API，设置现在可以被 Obsidian 设置搜索索引。
- 保留数据目录联想、Vault 路径检查、SecretStorage、数据库创建/载入/切换、备份/恢复和动态数据库状态反馈。
- `manifest.json` 的最低 Obsidian 版本提升到 1.13.0。安装或更新 v1.3.0 前，必须先将 Obsidian 更新到最新的 1.13.x 版本。
- 不改变现有 SQLite、`data.json` 或 SecretStorage 数据格式。

## 1.2.0 — 2026-07-31

- 国际化迁移为稳定语义键和独立中英文语言包，支持插值、复数、数字与日期格式，并在 CI 检查语言包覆盖和硬编码文案。
- 推荐模型改用 Web Worker 稀疏训练，加入截距、分层留出验证、自动/人工阈值、训练数据 hash、增量评分，以及作者、期刊、订阅源和新鲜度特征。
- 大规模稀疏特征宽度改为线性扫描，避免参数展开导致栈溢出；未读评分按实际命中特征查找权重，不再逐篇扫描完整词表。
- 关键词表移除高级人工权重，只保留带颜色区分的逐行停用/启用操作，人工停用状态在模型重建后继续保留；中文保留分词边界且不再生成带空格的二元短语，并自动过滤高频、正负样本分布近似的无区分力词。
- 关键词窗口改为按内容自适应宽度；每批订阅更新完成后自动刷新推荐，训练数据未变化时复用模型并仅增量计算新增或变化的未读文献。
- 文献相关度及推荐解释移至期刊信息下方的独立区域，避免与长标题互相挤压。
- 订阅更新支持 ETag、Last-Modified、HTTP 304、全局与同域有界并发、Retry-After、超时取消、健康状态和自动退避。
- 数据库 schema 升级为顺序迁移版本 3，保持现有 Vault 数据目录、保护性写入和显式载入生命周期。
- 同步更新中英文用户文档、开发说明、安全说明、故障排查和 1.2.0 发布说明。

## 1.1.1 — 2026-07-30

- 修复英文环境下五个文献篮子名称仍显示中文的问题；篮子标签改为渲染时按当前界面语言解析。
- 增加语言初始化后英文篮子标签的回归测试。
- 插件已上线社区插件市场，安装文档改为优先推荐通过 Obsidian 社区插件方式安装。
- 新增 Agent 接手指南、中英文故障排查，并补充启动顺序、状态所有权和 1.13 延期事项。

## 1.1.0 — 2026-07-30

- 新增中英文界面自动切换：中文语言环境显示简体中文，英文及其他语言环境回退英文。
- 设置、阅读器、命令、通知、动态进度、错误与无障碍文案统一接入界面语言层。
- v1.1.0 已在 Windows 和 macOS 桌面环境完成全部功能测试，并验证中文界面及英文回退。

## 1.0.4 — 2026-07-30

- 运行时数据库、迁移、备份和恢复改用 Obsidian Vault `DataAdapter`，不再直接调用 Node.js 文件系统。
- 路径统一为规范化的 Vault 相对路径，并拒绝绝对路径和 `..` 越界。
- 数据库继续采用临时文件保护替换，替换失败时恢复上一文件。
- 修正 manifest 英文描述及结尾标点，主 README 改为完整英文说明，并保留独立中文版本。
- 新增 tag 发布工作流，为三个受支持发布文件生成 GitHub artifact attestations。
- GitHub Release 只上传 `main.js`、`manifest.json`、`styles.css`；ZIP 和校验文件仅保留为本地产物。

## 1.0.3 — 2026-07-29

- 修复已打开 RSS 窗口随工作区恢复时阻塞 Obsidian 启动，以及首次打开只显示空标签的问题。
- 数据库载入和启动时订阅更新改为后台执行；自动更新会显示开始、完成或失败通知。
- 数据库切换与恢复增加统一任务协调，避免翻译、订阅、LLM 和推荐任务写入已关闭或已替换的数据库。
- 恢复数据库后重新载入未完成翻译队列，并正确显示等待、翻译中和失败状态。
- 加强 Vault 真实路径与 symlink 边界校验，目录联想不再产生 Vault 外建议。
- 统一异步按钮、导入和确认操作的错误处理，补充焦点样式、ARIA 状态、触摸尺寸及 popout 窗口兼容。
- 文献列表改为每批 100 条连续加载；文献篮子名称和数量改为单行布局。
- 关键词推荐计算分批让出主线程并优化批量写入，标题后显示彩色相关度标签，同层按关键词得分排序。
- 清理无效翻译配置和旧样式，新增生命周期、路径、数据库和 DOM 测试及 GitHub Actions CI。
- v1.0.3 已在 Windows 和 macOS 桌面环境完成全部功能测试。

## 1.0.2 — 2026-07-29

- 插件 ID 和名称改为 `academic-rss-reader` 与 Academic RSS Reader。
- LLM API Key 改用 Obsidian SecretStorage，并自动迁移旧 `data.json` 明文字段。
- 用户首次启用实验性标题翻译时，明确提示标题将发送给 Google。
- 用户输入的数据目录先经 Obsidian `normalizePath()` 规范化，再执行 Vault containment 检查。
- 阅读器与设置页按钮增加更明显的 hover 高亮、边框、阴影和位移反馈，并使用手型指针。
- 发布目录和 ZIP 改为 `academic-rss-reader/` 与 `Academic-RSS-Reader-1.0.2.zip`。
- 最低 Obsidian 版本提升到 1.11.4；项目继续使用 MIT License。

## 1.0.1 — 2026-07-28

- 自动更新改为每次启动后首次打开 RSS Reader 时静默执行，不再在 Obsidian 启动时弹出更新提示。
- 手动更新仍显示进度和结果，便于主动操作时确认状态。
- 插件启动时不再在插件目录创建或打开数据库；首次使用必须选择 Vault 内的数据目录。
- RSS 面板在数据库未配置时显示引导，数据库就绪后才启动业务服务。
- 支持创建、载入和安全切换数据库，迁移当前库时不会覆盖目标文件。
- 所有手动及危险操作保护备份统一保存到用户数据目录的 `backups/`。
- 损坏数据库只读校验失败时保留原文件，不创建插件目录恢复库。
- 正式版本不提供旧数据库导入功能。

## 1.0.0 — 2026-07-28

- 将 Streamlit RSS Reader 完整迁移为 Obsidian 桌面插件。
- 保留旧版 SQLite 核心表、stable GUID、标题规范化和分层去重规则。
- 提供订阅管理、五篮子阅读、撤回、推荐和兴趣分析。
- 增加视口标题翻译、后续 8 条预取和本地译文缓存。
- 增加 Vault 内数据库备份、恢复、损坏回退和一次性错误 GUID 修复。
- 发布包收敛为 `main.js`、`manifest.json`、`styles.css` 三个文件。
